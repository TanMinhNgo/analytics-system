export type Kpi = {
  label: string;
  value: number;
  change: number;
};

export type TimeseriesPoint = {
  label: string;
  value: number;
};

export type SourceBreakdown = {
  name: string;
  value: number;
};
